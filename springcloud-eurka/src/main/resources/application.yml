server:
  port: 8761

spring:
  application:
    name: eureka-server
eureka:
  server:
    enable-self-preservation : false
  instance:
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: false
